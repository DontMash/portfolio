---
import AccordionInteractive, { type AccordionItemData } from './accordion';

type Props = {
  multiple?: boolean;
  items: readonly AccordionItemData[];
};

const { multiple = false, items } = Astro.props;
---

<AccordionInteractive client:load {multiple} {items} />
