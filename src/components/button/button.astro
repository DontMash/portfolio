---
// biome-ignore lint/style/useImportType: <explanation>
import { Button as BitsButton } from 'bits-ui';
import { button } from './button';

type Props = BitsButton.Props;
const props = Astro.props;
---

<BitsButton.Root class={button()} {...props}>
  <slot />
</BitsButton.Root>
